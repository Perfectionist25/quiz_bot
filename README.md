# QuizzBot

Telegram-бот для тестов из `.txt` файлов:
- загрузка тестов пользователями;
- список всех тестов и список своих тестов;
- индивидуальное прохождение (каждый пользователь проходит отдельно);
- выбор таймера на каждый вопрос (`без таймера`, `10/15/20/30/45/60` секунд);
- итоговая подробная статистика по прохождению;
- личная статистика и общая статистика по всем тестам.

## Запуск

1. Создайте бота через `@BotFather` и получите токен.
2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Установите переменную окружения или создайте файл `.env`:

```bash
export BOT_TOKEN="ваш_токен"
```

или

```bash
echo 'BOT_TOKEN="ваш_токен"' > .env
```

4. Запустите:

```bash
python bot.py
```

После первого запуска рядом появится база `quizzbot.db`.

## Формат TXT

Поддерживается гибкий формат. Пример:

```txt
# Основы Python
1) Что выведет print(2 + 2)?
- 3
+ 4
- 5

2) Какой тип у результата 10 / 2?
- int
- str
- float *
```

Как отмечать правильный ответ:
- префикс `+` у строки ответа;
- или суффикс `*`;
- или суффикс `(+)`.

Требования:
- минимум 2 варианта ответа на вопрос;
- минимум 1 правильный вариант;
- файл в кодировке UTF-8;
- размер до 1MB.

## Кнопки в боте

- `📚 Список тестов`
- `➕ Создать тест из TXT`
- `🗂 Мои тесты`
- `📊 Моя статистика`
- `🌐 Общая статистика`
- `ℹ️ Формат TXT`

Во время теста:
- кнопки с вариантами ответа;
- `⛔ Завершить тест`.

После теста:
- `🔁 Пройти снова`
- `📚 К списку тестов`
- `🏠 Главное меню`
